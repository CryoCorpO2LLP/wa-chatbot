services:
  - type: web
    name: cryocorp-whatsapp-ai-bot
    env: node
    plan: free
    region: oregon
    buildCommand: "npm install"
    startCommand: "node index.js"
    autoDeploy: true
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: 3000
